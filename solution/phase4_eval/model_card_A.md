# Model Card - Visit Risk (Model A)

*Hospital Operations & Revenue Risk Intelligence Platform*  

- Model version: `1.0.0`  |  feature spec: v2  |  card generated: 2026-09-02
- Artefacts: `phase3_models/models/model_a.joblib` (+ `_calibrated.joblib`), `training_manifest.json`
- Selected estimator: **majority**; calibration: sigmoid on the validation month

## 1. Intent

**Purpose.** Track the *distribution* of visit risk (Low / Medium / High) across the network over time, as an input to capacity and staffing reviews.

**In scope.** Aggregate monitoring - e.g. 'the High-risk share in the ICU rose 4 points this quarter'. **Out of scope.** Prioritising, triaging or making any decision about an *individual* visit or patient. Model A has no per-visit discriminative power (section 4) and must not be presented to clinicians as a per-patient score.

## 2. Data

- **Source.** `capstone_solution.v_visit_billing` (Phase 1), as-of feature matrix `phase2_eda/output/feature_frame.parquet` (Phase 2).
- **Window.** 2025-01-20 -> 2026-01-20, 25,000 visits. Split: 9 months train / 1 validate / 2 test, calendar-anchored on visit_date.
- **Features.** 22 (13 numeric + 9 categorical). Target: `risk_score` (Low / Medium / High).
- **Leakage register.** `capstone.features` - `visit_date` is the only temporal key; no `approved_amount`, `payment_days`, `claim_status` or `billing_date` derivative. Model A additionally excludes `billed_amount` and `length_of_stay_hours` (not known / not operational at admission). Verified by ablation in `PHASE4_FINDINGS.md` section 5.

## 3. Metrics (test window)

| class   |   precision |   recall |     f1 |   support |
|:--------|------------:|---------:|-------:|----------:|
| Low     |      0.4972 |        1 | 0.6642 |      2118 |
| Medium  |      0      |        0 | 0      |      1279 |
| High    |      0      |        0 | 0      |       863 |

- Accuracy 49.7%, balanced accuracy 0.33 (= chance for 3 classes).
- **Recall on High-risk visits: 0%** - the model never predicts High.
- Model A's one-vs-rest ROC AUC is exactly 0.50 for every class and permutation importance is exactly 0 - shuffling any feature leaves balanced accuracy untouched (`PHASE4_FINDINGS.md` section 4.2 + appendix).

## 4. Thresholds

No operating threshold. Model A is read as a class *distribution*, not a per-visit decision. If a monitoring alert is ever built on it, the trigger is a shift in the aggregate High-risk share, not any individual prediction.

## 5. Limitations

- **No per-visit signal.** Phase 2's mutual-information screen and Phase 4's permutation importance both show no feature above the noise floor. `risk_score` is effectively randomly assigned in the source data.
- **Constant predictions.** The model predicts `Low` for every visit; its calibrated probabilities are just the training-window class frequencies.
- **Distribution monitoring only** is a weak use case - if the risk mix is stable (Phase 2 found it is), Model A carries little information even in aggregate. It is retained for completeness and interface parity with Model B, not because it is valuable.

## 6. Ethical considerations

- **Do-no-harm framing.** The main ethical risk is *misuse* - a constant model presented as a patient risk score would be misleading and could skew clinical attention. The card, the notebook and the findings all state that Model A is not a per-patient score.
- Fairness parity is trivially satisfied (identical constant output for every group). Monitored anyway in Phase 6 in case a future retrain finds signal.

## 7. Retraining triggers

Retrain (and re-derive the calibration and, for Model B, the operating threshold) when any of the following fires - these are the signals Phase 6 monitors on the prediction log:

- **Scheduled:** every 6 months, or whenever Phase 2's feature frame is rebuilt on new data - in case signal emerges that was not present in the pilot year.
- **Distribution drift:** PSI > 0.2 on the incoming risk-mix vs the training window (would indicate the monitored quantity itself has moved).
- **Signal check:** if a retrain ever produces a model that beats base-rate balanced accuracy by >= 0.02 on a time-held-out split, promote it from monitor to predictor and revisit this card.

---

*Generated by `phase4_eval/report.py` from `capstone.evaluation` on 2026-09-02T09:40:10+00:00. Do not hand-edit - re-run `phase4_eval/run_phase4.py`.*

